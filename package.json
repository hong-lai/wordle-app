{
  "name": "wordle-app",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": ["client", "server"],
  "scripts": {
    
    "start:cli": "tsx ./src/index.ts",
    "start": "npm run build && npm run start --workspace=server",
    "start:server": "npm run start --workspace=server",
    "start:client": "serve -l 5173 client/dist",

    "dev:server": "npm run build:server && npm run dev --workspace=server",
    "dev:client": "npm run build:client && npm run dev --workspace=client",

    "build": "npm run build:client && npm run build:server && cp -r client/dist server/dist/server/src/public",
    "build:server": "npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",

    "test": "tsx --experimental-test-coverage --test src/tests/**/*.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=23.11.1"
  }
}
